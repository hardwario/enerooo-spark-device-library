# Generated by Django 6.0.4 on 2026-05-12 11:07

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('library', '0029_processorconfig_extra_mappings_and_more'),
    ]

    operations = [
        migrations.AlterField(
            model_name='processorconfig',
            name='extra_mappings',
            field=models.JSONField(blank=True, default=list, help_text="Per-model extras — metrics this specific VendorModel produces that aren't declared on the parent DeviceType and that the operator chose not to promote into the L1 catalogue. Each entry: {source, target, tier, label?, unit?, scale?, offset?}. ``tier`` is per-entry (primary / secondary / diagnostic). ``label`` and ``unit`` live on the entry itself (no L1 row required) — if the target happens to match an existing L1 key, the catalogue's label/unit is used as a fallback. Targets are NOT auto-created in L1 for this list."),
        ),
    ]
