# Generated by Django 5.1.11 on 2026-05-12 09:26

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ("library", "0027_field_mappings_drop_tags"),
    ]

    operations = [
        migrations.AlterField(
            model_name="processorconfig",
            name="field_mappings",
            field=models.JSONField(
                blank=True,
                default=list,
                help_text="L4 — list of {source, target, scale?, offset?} entries mapping this model's decoded fields onto canonical L1 Metric keys (``target`` is the Metric.key being pointed at). ``scale`` (default 1) and ``offset`` (default 0) apply a linear conversion ``value * scale + offset`` — used when the decoder can't emit canonical units (typically vendor LoRaWAN codecs we don't fork). Multi-channel devices model each channel as a separate metric (e.g. ``elec:voltage_l1`` / ``elec:voltage_l2`` / ``elec:voltage_l3``). Entries referencing a target key not yet in the L1 catalogue auto-create the Metric row on save (operators tidy label/unit in admin afterwards).",
            ),
        ),
    ]
