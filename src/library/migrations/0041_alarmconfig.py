# Generated by Django 6.0.4 on 2026-07-02 18:27

import uuid

import django.db.models.deletion
import django.utils.timezone
import model_utils.fields
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('library', '0040_remove_processorconfig_extra_config'),
    ]

    operations = [
        migrations.CreateModel(
            name='AlarmConfig',
            fields=[
                ('created', model_utils.fields.AutoCreatedField(default=django.utils.timezone.now, editable=False, verbose_name='created')),
                ('modified', model_utils.fields.AutoLastModifiedField(default=django.utils.timezone.now, editable=False, verbose_name='modified')),
                ('id', models.UUIDField(default=uuid.uuid4, editable=False, primary_key=True, serialize=False)),
                ('mappings', models.JSONField(blank=True, default=list, help_text="List of {source?, match, severity, description?} entries mapping device-reported status flags to alert severities. ``source`` is the decoded field to inspect (default ``status`` — the wmbusmeters convention); ``match`` is the flag token / value that activates the alarm (matched as a token within space/comma-separated multi-flag strings, or by equality); ``severity`` is one of info | warning | critical. Consumers raise/auto-resolve alerts from these at ingest time; flags not listed here fall back to the consumer's built-in driver registry, then to a generic warning.")),
                ('device_type', models.OneToOneField(on_delete=django.db.models.deletion.CASCADE, related_name='alarm_config', to='library.vendormodel')),
            ],
            options={
                'abstract': False,
            },
        ),
    ]
