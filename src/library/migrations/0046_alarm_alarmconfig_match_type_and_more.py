# Generated by Django 5.1.11 on 2026-08-06 14:14

import django.utils.timezone
import model_utils.fields
import uuid
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ("library", "0045_gatewayassignment_name"),
    ]

    operations = [
        migrations.CreateModel(
            name="Alarm",
            fields=[
                (
                    "created",
                    model_utils.fields.AutoCreatedField(
                        default=django.utils.timezone.now,
                        editable=False,
                        verbose_name="created",
                    ),
                ),
                (
                    "modified",
                    model_utils.fields.AutoLastModifiedField(
                        default=django.utils.timezone.now,
                        editable=False,
                        verbose_name="modified",
                    ),
                ),
                (
                    "id",
                    models.UUIDField(
                        default=uuid.uuid4,
                        editable=False,
                        primary_key=True,
                        serialize=False,
                    ),
                ),
                (
                    "key",
                    models.CharField(
                        help_text="Canonical key, e.g. 'water:leak'. '<namespace>:<name>' convention recommended, not enforced.",
                        max_length=128,
                        unique=True,
                    ),
                ),
                ("label", models.CharField(max_length=128)),
                (
                    "default_severity",
                    models.CharField(
                        choices=[
                            ("info", "Info"),
                            ("warning", "Warning"),
                            ("critical", "Critical"),
                        ],
                        default="warning",
                        help_text="Severity a mapping inherits unless it sets its own override.",
                        max_length=10,
                    ),
                ),
                ("description", models.TextField(blank=True, default="")),
            ],
            options={
                "ordering": ["key"],
            },
        ),
        migrations.AddField(
            model_name="alarmconfig",
            name="match_type",
            field=models.CharField(
                choices=[("flag", "Flag"), ("equals", "Equals"), ("mask", "Mask")],
                default="flag",
                help_text="How ``match`` values are evaluated against the source field: ``flag`` = token in a flag-list string (wmbusmeters status); ``equals`` = value equality (LoRaWAN error codes, enums, booleans); ``mask`` = bitmask test ``(value & mask) == pattern``.",
                max_length=10,
            ),
        ),
        migrations.AlterField(
            model_name="alarmconfig",
            name="mappings",
            field=models.JSONField(
                blank=True,
                default=list,
                help_text="List of {source?, match, alarm?, severity?, description?} entries mapping device-reported status flags/values to L1 Alarm identities. ``source`` is the decoded field to inspect (default ``status`` — the wmbusmeters convention); ``match`` activates the alarm per the config's ``match_type``; ``alarm`` points at the shared catalogue (severity/description inherited from it unless overridden here). Consumers raise/auto-resolve alerts from these at ingest time; flags not listed here fall back to the consumer's built-in driver registry, then to a generic warning.",
            ),
        ),
    ]
